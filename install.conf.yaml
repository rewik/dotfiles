- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.config/fish: fish
    ~/.zshrc: zshrc
    #~/.config/astronvim: astronvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
