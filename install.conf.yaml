- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.config/astronvim: astronvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
